---
export interface Props {
  title: string;
}

const { title } = Astro.props;
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link
      rel="icon"
      href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><text y=%22.9em%22 font-size=%2290%22>👨‍🌾</text></svg>"
    />
    <meta name="generator" content={Astro.generator} />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Open+Sans:wght@400;500;600;700;800&display=swap"
      rel="stylesheet"
    />
    <title>{title}</title>
  </head>
  <body>
    <header>
      <div class="max-width">
        <a href="/">Hem</a>
        <a href="/slakttrad">Släktträd</a>
        <a href="/historia">Historia</a>
        <a href="/historia">Släktforskning</a>
      </div>
    </header>
    <slot />
    <footer>
      <div class="max-width">
        <div class="prose">
          <address>Brinkepojkarna</address>
        </div>
      </div>
    </footer>
  </body>
</html>
<style is:global lang="scss">
  body {
    margin: 0;
    background-color: #faf7fa;
  }

  .max-width {
    max-width: 78.75rem;
    margin: 0 auto;
  }

  header {
    font-weight: 600;
    padding: 10px 0;

    .max-width {
      display: flex;
      flex-flow: row wrap;
      padding: 0 15px;
    }

    a {
      display: block;
      height: 50px;
      line-height: 50px;
      padding: 0 20px;
      text-align: center;

      &:hover {
        text-decoration: underline;
      }
    }
  }

  img {
    max-width: 100%;
    object-fit: cover;
  }

  section {
    > * {
      padding: 50px 30px;
    }
  }

  .content {
    background-color: #fff;
  }

  .btn {
    @apply inline-flex items-center justify-center px-5 py-3 text-base font-medium text-center text-gray-100 rounded-lg focus:ring-4 focus:ring-stone-300;
    text-decoration: none;
    background-color: #3e4543;
    font-weight: 600;
    &:hover {
      background-color: darken(#3e4543, 5);
    }
  }

  main {
    min-height: 70vh;
  }

  footer {
    min-height: 215px;
    background-color: #e7e2d8;
    > * {
      padding: 50px 30px;
    }
  }
</style>
